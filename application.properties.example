# ============================================================================
# SEO CONFIGURATION EXAMPLES - Music Hub Application
# ============================================================================

# 🎯 Development Environment Configuration
# ============================================================================

# Server Configuration
server.port=8080
server.servlet.context-path=/

# ============================================================================
# DATABASE CONFIGURATION - MySQL
# ============================================================================

# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/bai_2?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# ============================================================================
# SEO CONFIGURATION - Update these for your site
# ============================================================================

# Site Information
app.seo.siteName=Music Hub
app.seo.siteUrl=http://localhost:8080
app.seo.description=Music Hub - Your gateway to music management. Explore thousands of artists, albums, and songs.
app.seo.twitterHandle=@musichubblog
app.seo.siteLanguage=en
app.seo.ogImage=/images/logo.png

# ============================================================================
# PERFORMANCE OPTIMIZATION
# ============================================================================

# Enable GZIP Compression
server.compression.enabled=true
server.compression.min-response-size=1024
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json,application/xml+rss,application/rss+xml

# Thymeleaf Cache (disable in dev, enable in prod)
spring.thymeleaf.cache=false
spring.thymeleaf.mode=HTML

# JPA/Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# ============================================================================
# DATABASE MIGRATION (Flyway)
# ============================================================================

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=true

# ============================================================================
# FILE UPLOAD CONFIGURATION
# ============================================================================

app.upload.dir=D:/Workspace/Study/SelfStudy/code_for_FUN/Learn_SpringBoot/web_seo/upload/

# ============================================================================
# APPLICATION INFORMATION
# ============================================================================

spring.application.name=Bai_2
spring.application.version=1.0.0


# ============================================================================
# ⬇️ PRODUCTION CONFIGURATION EXAMPLE ⬇️
# ============================================================================
# Use this for production deployment
# Replace values with your actual production settings

# --- BEGIN PRODUCTION SECTION (COMMENTED) ---

# server.port=8443
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=your-password
# server.ssl.key-store-type=PKCS12

# SEO Configuration for Production
# app.seo.siteName=Music Hub
# app.seo.siteUrl=https://musichubblog.com
# app.seo.description=Music Hub - Discover Your Favorite Artists, Albums & Songs
# app.seo.twitterHandle=@musichubblog
# app.seo.siteLanguage=en
# app.seo.ogImage=https://musichubblog.com/images/logo.png

# Database Configuration for Production (MySQL)
# app.datasource.url=jdbc:mysql://prod-mysql-server:3306/musicHub_prod?useSSL=true&serverTimezone=UTC
# app.datasource.username=${DB_USERNAME}
# app.datasource.password=${DB_PASSWORD}
# app.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# app.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Connection Pool for Production
# spring.datasource.hikari.maximum-pool-size=30
# spring.datasource.hikari.minimum-idle=10

# Caching enabled for production
# spring.thymeleaf.cache=true

# Logging configuration
# logging.level.root=WARN
# logging.level.doan.bai_2=INFO
# logging.file.name=/var/log/musicHub.log

# File upload for production
# app.upload.dir=/home/appuser/uploads/

# --- END PRODUCTION SECTION ---


# ============================================================================
# DEVELOPMENT TOOLS CONFIGURATION
# ============================================================================

# Spring DevTools
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

# ============================================================================
# MONITORING & ACTUATOR (Optional)
# ============================================================================

# management.endpoints.web.exposure.include=health,metrics,env
# management.endpoint.health.show-details=when-authorized

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# Error Handling
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=on_param
server.error.include-exception=false

# ============================================================================
# NOTES FOR SEO
# ============================================================================
# 
# 1. DEVELOPMENT (localhost):
#    - Set app.seo.siteUrl=http://localhost:8080
#    - Disable HTTPS
#    - Use local database
#
# 2. PRODUCTION (yourdomain.com):
#    - Change app.seo.siteUrl to https://yourdomain.com
#    - Enable HTTPS/SSL
#    - Use production database
#    - Enable Thymeleaf cache
#    - Update Open Graph image to full URL
#    - Submit sitemap to Google Search Console
#    - Setup Google Analytics
#
# 3. IMPORTANT:
#    - Update Twitter handle with your actual handle
#    - Update OG image with your actual logo
#    - Use environment variables for sensitive data
#    - Never commit passwords to version control
#
# ============================================================================
